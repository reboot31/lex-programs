#include <stdio.h>

int main()
{
	int a;
	float b;
	char c;
	scanf("%d %f %c", &a, &b, &c);
	printf("%d %f %c", a, b, c);
	for (int i=0; i<a; i++)
		do something
			while you work
	if this
		then this
	else
		this
	return 0;
}